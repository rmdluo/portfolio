---
import Card from './Card.astro';
import type { CardProps } from '../../types/CardProps';

const { title, description, tags, class: className } = Astro.props as CardProps & { class?: string };

// Combine our classes with any passed in classes
const combinedClasses = ['hoverable-card', className].filter(Boolean).join(' ');
---

<Card 
    title={title}
    description={description}
    tags={tags}
    classes={[combinedClasses]}
>
    <slot />
</Card>

<style>
    :global(.hoverable-card) {
        text-decoration: none;
        color: inherit;
        display: block;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

    :global(.hoverable-card:hover) {
        transform: translateY(-5px);
        box-shadow: 0 4px 20px rgba(50, 69, 255, 0.1);
    }
</style> 